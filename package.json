{
  "name": "federated-learning-app",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "deploy": "npm run build --prefix frontend && npm run deploy --prefix frontend"
  }
}
